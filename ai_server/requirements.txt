# ============================================================
# ai_server/requirements.txt
#
# IMPORTANT: This must be installed in a Python 3.11 venv.
# Python 3.14 is NOT compatible with PyTorch/Whisper CUDA wheels.
#
# Setup:
#   1. From the ai_server/ directory:
#      py -3.11 -m venv venv
#      venv\Scripts\activate
#
#   2. Install PyTorch (Whisper requires it — CPU build, safe on all machines):
#      pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
#
#   3. Install remaining dependencies:
#      pip install -r requirements.txt
#
# LLM: Groq cloud API (free tier — 14,400 req/day)
#   Requires GROQ_API_KEY in ai_server/.env
#   Get key free at: https://console.groq.com
#
# Local Mistral GGUF kept at:
#   C:\Users\saadh\Models\mistral-7b-instruct-v0.2.Q4_K_M.gguf
#   (reserved for offline mode — not loaded by default)
# ============================================================

# Web framework
flask==3.0.3
flask-cors==4.0.1
click==8.1.3
colorama==0.4.6

# Speech-to-text (Whisper)
openai-whisper==20250625

# PyTorch (required by Whisper — CPU + CUDA auto-detected)
# Install BEFORE running: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.2.0
torchaudio>=2.2.0

# LLM inference — Groq cloud API (free tier, replaces local llama-cpp-python)
# Get a free key at: https://console.groq.com
# Add to ai_server/.env: GROQ_API_KEY=your_key_here
groq

# TTS — Azure Neural (Primary)
azure-cognitiveservices-speech

# TTS - gTTS (for native language text fallback)
gtts

# TTS - edge-tts (backup)
edge-tts

# Utilities
numpy>=1.24.0
python-dotenv==1.0.1
